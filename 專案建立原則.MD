# 專案建立原則

- 禁止使用任何EMOJI，使用相近意義的SVG替代裝飾/圖像
- 請注意專案內相似之按鍵/欄目/輸入框/選單，新製作項目參照相似風格製作
- 美化務必考量同欄位物件達到對齊。如按鈕與輸入欄水平對齊、標題與圖樣水平對齊、欄位中內容簡易則在此欄位置中對齊
- 色彩皆使用ROOT，不容許任何獨立的色彩顯示
- CSS務必同時考量IOS系統與電腦/安卓的兼容度
  - 加上 `-webkit-text-size-adjust: none` 防止 iOS 縮放文字
  - `hover` 效果需提供 `@media (hover: none)` 替代方案
  - Mobile-first：先設計 `375px`，以 `min-width` media query 向上擴展
- 以簡潔的代碼來編輯邏輯，隨時刪減不必要的冗贅代碼
- 資料儲存系統環環相扣且穩固
- 以記憶體最優化順暢為原則
