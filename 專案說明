## 專案：紙娃娃換裝

- Vue 3 + Pinia + Vite 的前端專案，提供匯入圖包後的換裝/搭配體驗，資料持久化於 IndexedDB。
- Web Worker + JSZip 處理 ZIP 圖包，避免主執行緒阻塞。
- 另有獨立的圖包打包器 `紙娃娃打包器.html` 供創作者產出含 `config.json` 的 ZIP。

### 技術棧與執行
- Vue 3 (script setup)、Pinia 狀態樹、Vite 開發/打包。
- IndexedDB：`core/index.js` 封裝 stores `items` / `outfits` / `packs`（DB v3）。
- Web Worker：`public/zip.worker.js` + `jszip.min.js` 處理上傳與進度回報。
- 指令：`npm install` → `npm run dev`；`npm run build` 產出靜態檔。

### 主要模組與職責
- `App.vue`：兩頁切換（換裝/房間）、頂欄搜尋與設定彈窗、響應式佈局（桌機/平板/手機）。
- `Wardrobe.vue`：左側衣櫃；分類側欄、圖包多選篩選、Tag 篩選、排序、虛擬滾動、行動版水平滾動；支援長按/右鍵開啟變體選單；星號類別對應已儲存搭配。
- `Dressing.vue`：畫布（固定/自由模式）、背景/人物/前景分層、選取高亮、鍵盤快捷鍵、縮放/平移、自由拖曳/縮放/翻轉/旋轉（非角色）。
- `Controls.vue`：浮動控制（清空、重置變換、模式切換、縮放、啟用旋轉/縮放、左右/上下翻轉、Undo/Redo、儲存搭配）。
- `LayerPanel.vue`：當前畫布物件列表，點選選取、拖曳或按鈕調整層級、可重置自訂順序。
- `Room.vue`：已儲存搭配的卡片牆；可載入、複製、刪除；目前僅預覽/載入。(預計未來提供自由擺放多角色的房間編輯)
- `Search.vue`：全域搜尋物件/搭配/圖包（含 Tag 比對），可直接穿戴/載入/查看。
- `Settings.vue`：圖包管理（開啟 Importer、刪除圖包）、清空所有本地資料。
- `Importer.vue`：ZIP 匯入介面，拖放或選檔；顯示進度、錯誤提示、成功提示；呼叫 Worker 解析後寫入 IndexedDB。
- `core/index.js`：IndexedDB 初始化與 CRUD、刪除圖包連動刪物件、清庫；版本升級時重建 stores。
- `store/index.js`：全域狀態、換裝邏輯、Undo/Redo（上限 50 步）、自由模式座標/縮放/翻轉/旋轉、圖層排序、通知、UI 折疊/響應式旗標。
- `public/zip.worker.js`：讀取 ZIP → 解析 `config.json` → 生成 items + packInfo，支援變體圖片；以 `progress`/`success`/`error` 回傳。
- `紙娃娃打包器.html`：JSZip 製包工具（網頁獨立執行），協助作者產出符合 `config.json` 規格的圖包。


### 開發提示
- 重新初始化資料時請留意 IndexedDB 版本號位於 `core/index.js`（目前 v3）。
- Undo/Redo 最多保留 50 筆狀態；涉及自由模式的操作請呼叫 `recordHistory()`。
- 在套用變體時會優先使用 `variantImages` 中的圖片；Importer 會自動套預設變體圖片。
- 若調整類別或槽位名稱，需同步更新 `slotNameMap`、`categoryOrder`、`defaultZIndexMap` 和 `categories`。